# Example 10 — Unified State Callbacks

Run:

```bash
python examples/10_state_callbacks/unified_state_callbacks.py
```

Covers:
- `on_any_change(...)`
- `on_lifecycle_change(...)`
- lifecycle + operational + health transitions
- transition history via `get_history()`
