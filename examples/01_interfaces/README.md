# Example 01 - interfaces

This top-level example is split into two parts:

- `implementation/`: callback implementation examples (message, service, action)
- `configuration/`: interface metadata JSON examples and loading workflow

Use this folder as the baseline when implementing module interfaces.
